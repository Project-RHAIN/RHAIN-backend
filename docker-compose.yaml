version: '3.9'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"    
  frontend:
    build: 
      context: ../RHAIN-frontend/app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"